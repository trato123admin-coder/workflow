# Integraciones

Antes del **Sprint 9**, guardar aquí `apiinti.md` con la documentación de APIINTI copiada desde su panel:
URL base, forma de autenticación, endpoints de DNI y RUC y un ejemplo de respuesta de cada uno.

**No incluir la API key en este archivo.** La clave va solo en la variable de entorno `APIINTI_API_KEY`.
